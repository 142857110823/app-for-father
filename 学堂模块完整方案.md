# 道家奇门遁甲 APP：学堂模块完整方案

日期：2026-08-24

## 1. 目标

将现有“书籍简介列表”升级为可实际使用的移动端电子书学堂：

- 首次进入学堂时下载书籍，下载完成后永久离线保存。
- 首批不少于 8 部与奇门遁甲、三式及其理论基础相关的合法电子版古籍。
- 支持书架、下载状态、目录、正文阅读、字号/主题调整、阅读进度和断点续读。
- Android 8.0+ 可用；断网后可打开已下载书籍。
- 保留来源页、许可说明、下载时间、文件大小和 SHA-256 校验值。

## 2. 版权与内容边界

只收录满足以下条件之一的资源：

1. 原作已进入公版领域，电子文本来自明确标注许可的开放平台。
2. 权利人明确授权再分发。
3. 开放许可允许复制、缓存和在 APP 内阅读。

首批内容采用维基文库及其官方导出服务。古籍原作属于公版文本；维基文库整理文本按页面所示许可使用。APP 在书籍详情页保留作品来源、导出来源和署名/许可说明。来源不明的网盘、论坛附件、盗版 PDF、现代商业出版物不纳入书库。

## 3. 首批书目

| ID | 书名 | 类别 | 来源 |
|---|---|---|---|
| qimen-tongzong | 奇門遁甲統宗 | 奇门主典 | 中文维基文库 |
| qimen-yuanling | 奇門遁甲元靈經 | 奇门主典 | 中文维基文库 |
| dunjia-yanyi | 遁甲演義（四庫全書本） | 奇门主典 | 中文维基文库 |
| qimen-baojian | 奇门宝鉴御定 | 奇门主典 | 中文维基文库 |
| yanbo-diaosou | 煙波釣叟歌 | 奇门歌诀 | 中文维基文库 |
| huangting-dunjia | 黃庭遁甲緣身經 | 遁甲道经 | 中文维基文库 |
| taiyi-jinjing | 太乙金鏡式經（四庫全書本） | 三式旁参 | 中文维基文库 |
| liuren-daquan | 六壬大全（四庫全書本） | 三式旁参 | 中文维基文库 |

每本书使用 `ws-export.wmcloud.org` 的 EPUB 导出结果，并以中文维基文库作品页作为可阅读的来源说明页。实施阶段生成固定清单，不在运行时搜索互联网。

## 4. 方案比较

### 方案 A：EPUB + IndexedDB（采用）

- EPUB 是标准电子书格式，适合手机屏幕重排、目录和阅读进度。
- 首次下载后以二进制 Blob 保存到 IndexedDB。
- 使用本地打包的 EPUB.js 渲染，不依赖在线 CDN。
- APK 不包含书籍正文，体积稳定；新增书籍只需更新清单。

### 方案 B：转换为自有 HTML/JSON

- 开发简单、渲染快，但会破坏标准电子书结构。
- 需要自行处理章节、脚注、图片、目录和文本更新。
- 不采用。

### 方案 C：PDF 阅读

- 保留扫描版版式，但手机阅读需要频繁缩放。
- PDF.js 和扫描文件会显著增加下载量与内存占用。
- 不作为首期方案。

## 5. 架构

### 5.1 文件职责

- `school-library.js`：书目清单、下载状态机、IndexedDB 存储、校验、阅读进度。
- `school-reader.js`：EPUB.js 阅读器、目录、翻页、字号、主题、进度恢复。
- `school.css`：学堂书架、下载状态、阅读器及移动端适配。
- `vendor/epub.min.js`：本地 EPUB.js 运行库。
- `vendor/jszip.min.js`：EPUB.js 所需的本地压缩解析库。
- `tests/school-library.test.js`：清单、状态、下载、校验、失败恢复和进度测试。
- `index.html`：保留现有入口，移除占位书籍数据，接入独立学堂模块。
- `public/`、`docs/`：与根目录前端保持同步。

### 5.2 数据模型

书目记录：

```js
{
  id,
  title,
  author,
  category,
  description,
  sourcePage,
  downloadUrl,
  license,
  expectedType: "application/epub+zip"
}
```

本地书籍记录：

```js
{
  id,
  blob,
  bytes,
  sha256,
  downloadedAt,
  sourceUrl,
  version
}
```

阅读设置与进度：

```js
{
  bookId,
  cfi,
  percentage,
  fontScale,
  theme,
  updatedAt
}
```

## 6. 用户流程

1. 用户首次进入“学堂”。
2. APP 显示 8 本书的书架和总下载量预估，不强制阻塞整个页面。
3. 默认自动依次下载；用户也可暂停、重试或单独下载。
4. 每本书完成后立即写入 IndexedDB，并记录大小与 SHA-256。
5. 点击已下载书籍进入 EPUB 阅读器。
6. 阅读器自动恢复上次位置；翻页后节流保存进度。
7. 后续断网进入学堂时直接读取本地书库。
8. 清除单本缓存后可重新下载；应用数据被系统清除或卸载后需要重新下载。

“永久离线”在 Android 平台上的准确含义：只要用户不卸载 APP、不清除 APP 数据，已下载书籍会持续保存在设备本地。

## 7. 界面

- 沿用宣纸白、墨色、暗金配色和楷体优先策略。
- 首屏直接展示书架，不增加营销式页面。
- 每本书显示：书名、类别、简介、大小、下载/已下载/失败状态。
- 下载使用图标按钮和进度条；失败状态提供明确重试。
- 阅读器顶部为返回、书名、目录、显示设置；正文区域不嵌套卡片。
- 支持浅色、护眼、深色三种阅读主题和 4 档字号。
- 右下角 AI 阴阳双鱼按钮继续跨页面常驻，但阅读器打开时不遮挡正文和控制栏。

## 8. 网络与异常处理

- 下载并发固定为 2，避免首启占满带宽。
- 单本下载失败不影响其他书；自动重试 2 次，之后显示手动重试。
- 验证 HTTP 状态、MIME 类型、最小文件长度和 EPUB ZIP 文件头。
- 下载中断不写入“已完成”状态，避免半文件进入书架。
- 存储空间不足时停止后续下载并提示用户清理空间。
- 书籍清单和已下载数据分开，清单更新不会删除本地书。
- 网络不可用时不反复弹窗，只在书架状态栏说明“离线，可阅读已下载内容”。

## 9. 测试与验收

自动测试：

- 书目不少于 8 本且 ID、URL 唯一。
- 下载状态按 `idle -> downloading -> ready/error` 转换。
- 非 EPUB、空文件和失败响应不会写入本地书库。
- SHA-256、大小、下载时间正确记录。
- 阅读位置与显示设置可保存和恢复。
- 离线状态下已下载书籍可读取，未下载书籍给出明确状态。

构建与人工验证：

- 运行现有算法测试，确认学堂改动不影响排盘。
- 构建浏览器资源并同步 Capacitor Android 工程。
- 在桌面浏览器验证 360x800、412x915 和桌面视口。
- 使用 Windows 浏览器自动化验证下载、打开、目录、翻页、字号、断网提示。
- 构建 APK，并确认 Android 8.0 最低版本配置未被破坏。
- 在 `work-flow.md` 记录时间、事件、问题来源、执行方向、边界、结果和相关文档。

## 10. Git 与交付边界

- 仓库：`https://github.com/142857110823/app-for-father`
- 当前分支：`master`
- 不回退或覆盖工作区中已有的算法、排盘和三分区界面修改。
- 根目录、`public/`、`docs/` 三处前端内容保持一致。
- 最终交付包括源代码、测试、更新后的 APK、书目与许可清单、工作流记录。
