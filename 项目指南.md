工作方式
1. 先理解我的真实目标，而不是只按字面回复。
2. 如果我的表达混乱、跳跃、信息不完整，你要先主动拆解出：
   - 显式任务
   - 隐含任务
   - 缺失信息
   - 关键约束
   - 最终交付物
3. 只有在关键信息缺失、会影响执行方向时，才向我提问；问题必须少而关键，最多 1-3 个。
4. 一旦任务足够清楚，不要停在“给建议”或“优化 prompt”，而是直接开始执行。
5. 执行过程中，始终把“事实依据、当前进度、下一步动作、剩余风险”说清楚。
6. 如果需要最新信息、规范、案例、竞赛资料、PPT 表达方式、相关领域对标内容，必须联网搜索后再回答，不允许凭印象编造。
7. 如果事实无法确认，要明确说“无法确认”，不要补造。
8. 如果发现我真正要解决的问题和我表面说的不一致，你要指出并按真正目标推进。

# 十三宫奇门遁甲 APP —— 项目指南

## 一、项目功能

十三宫奇门遁甲 APP 是一款面向 Android 手机的**阴盘奇门遁甲排盘工具**，基于传统洛书九宫衍生出的十三宫体系，支持阳遁/阴遁定局、神/星/门全要素排布，并提供历史记录与结果导出能力。

### 1.1 核心功能

| 功能模块 | 说明 |
|----------|------|
| 起局输入 | 输入姓名、性别、出生时间（年月日时分）、出生地点、真太阳时开关、阴盘/阳盘选择 |
| 四柱干支 | 根据公历时间自动计算年柱、月柱、日柱、时柱 |
| 遁局判定 | 依据前言算法判定阳遁/阴遁、第几局（0-9 局） |
| 十三宫排盘 | 按 4×4 传统表格展示 13 个宫位，中心宫位合并；每宫采用**左/中/右三分区**布局：左=神/星/门，中=灵/天/人/地干，右=天罡/日排局 |
| 神/星/门/干全要素 | 神：玄武→白虎→太常→六合→勾陈→腾蛇→玄灵→天后→九天→太阴→贵神→青龙→朱雀；星：13星；门：13门；天盘、地盘（人盘）、暗干（辅助校验，不显示） |
| **灵盘系统** | 神盘宫位 → 神原始宫位 → 取地盘干（2(1)(1) 文档 TABLE 16） |
| **天罡系统** | 12要素（天罡、太乙、腾光、小吉、传送、从魁、河魁、登时、神后、大吉、功曹、太冲），按农历月份+时支查表定位起始宫，顺时针填入外围 12 宫 |
| **日排局系统** | 依据农历月/日，为 1/4/7/10 月的 3 日期模式或其余月份的 2 日期模式宫位，添加"正月 X日" 等标签（最高规则：1/2/3/29/30/31 对应 1 月起始宫子宫） |
| 宫位解析 | 点击任意宫位弹出该宫神、星、门、天干、灵盘、天罡、日排局等详细解析 |
| 历史记录 | 自动保存每次排盘结果，支持查看、删除、导出 JSON |
| PDF 导出 | 将排盘结果（信息、四柱、遁局、分析、图表）导出为可打印 HTML/PDF |
| 我的页面 | 统计排盘总数、阳遁/阴遁数量，提供夜间模式/我的收藏/我的消息/导出历史/清空历史入口 |

### 1.2 输入与输出

- **输入**：用户填写的求测信息 + 系统当前时间（此刻按钮/示例按钮）；算法层需额外携带 `extraContext = { lunarMonth, lunarDay, shiZhi }`（农历月份、农历日期、时支地支）。
- **输出**：四柱干支、遁局信息、十三宫图表（三分区展示）、每宫字段 `{ shen, xing, men, tianGan, diGan, anGan, lingGan, tiangang, riPaiJu }`、宫位解析、历史记录。

---

## 二、项目目标与方向

### 2.1 项目目标

1. **准确性**：排盘算法 100% 符合以下权威文档示例：
   - 示例①：前言.docx「阴盘-阳遁-5局」（丙午 丙申 庚申 癸未）
   - 示例②：排盘-【阴盘-阴遁-5局】 2(1)(1).docx（丙午 丙申 庚申 壬午）
   - 神/星/门/天/地/暗干 + 灵盘（TABLE 16）+ 天罡 + 日排局逐宫校验
   - 参考来源补充：`天罡.docx`（天罡系统表与日排局月配置）
2. **可安装性**：输出可在 Android 8.0+ 正常安装运行的 APK。
3. **离线可用**：APP 内嵌算法，无需联网即可排盘（PDF 导出调用系统浏览器）。
4. **简洁专业**：参考夸克扫描王风格与中国传统审美，界面干净、层级清晰、无无关广告/文本；十三宫每宫三分区（左=神/星/门，中=灵/天/人/地，右=天罡/日排）。
5. **可维护性**：代码结构清晰，算法模块与 UI 严格分层，`algorithm/` 单独可单测，修改后通过 `node algorithm/test.js` 与 `node algorithm/pillars.js` 验证，重新运行 `npm run build:browser` 生成 bundle。

### 2.2 当前方向

- **已完成**：前端 UI 重构、浏览器端算法打包、Capacitor Android 工程、GitHub Actions 自动构建 APK。
- **待完善**：
  - 真太阳时按地点自动校准的具体算法。
  - 阳盘体系排盘规则（当前仅实现阴盘，阳盘选择为数据占位）。
  - 算法单元测试覆盖与边界case验证。
  - 发布签名 APK（当前 GitHub Actions 输出 debug 与 unsigned release）。

---

## 三、技术架构

### 3.1 技术栈

| 层级 | 技术 |
|------|------|
| 前端 UI | HTML5 + CSS3 + Vanilla JavaScript |
| 排盘算法 | Node.js 模块（lunar-javascript + 自定义十三宫算法） |
| 浏览器算法 | esbuild 打包为 IIFE bundle |
| 本地预览 | Node.js + Express（server.js） |
| 移动端封装 | Capacitor 3.x + Android Gradle |
| CI/CD | GitHub Actions |

### 3.2 运行模式

```
┌─────────────────────────────────────────────────────────────┐
│  Android APP (Capacitor WebView)                            │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  public/index.html                                    │  │
│  │  ├─ 加载 knowledge.js（神/星/门知识库）              │  │
│  │  ├─ 加载 algorithm.bundle.js（浏览器算法包）         │  │
│  │  └─ 调用 QiMenAlgorithm.fullPaiPan() 进行排盘        │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┴─────────────────────┐
        ▼                                           ▼
  开发模式：node server.js                    生产/离线：直接本地加载
  通过 /api/paipan 调用算法                    无需服务器
```

---

## 四、文件关系与交互链路

### 4.1 目录结构

```
f:\1\夫\
├── algorithm/                 # 排盘算法（Node.js 模块）
│   ├── browser-entry.js       # 浏览器打包入口
│   ├── knowledge.js           # 神/星/门/空亡/驿马等知识数据
│   ├── pillars.js             # 四柱干支计算 + 完整排盘入口
│   └── qimen.js               # 十三宫核心排盘算法
├── public/                    # Web 前端资源（Capacitor webDir）
│   ├── algorithm.bundle.js    # esbuild 打包后的浏览器算法（构建生成）
│   ├── index.html             # 主页面（起局/结果/历史/我的）
│   └── knowledge.js           # 前端知识库（与 algorithm/knowledge.js 同源）
├── android/                   # Capacitor 生成的 Android 工程
│   ├── app/                   # App 模块源码、资源、Gradle 配置
│   └── gradlew / build.gradle # Gradle 构建脚本
├── .github/workflows/         # GitHub Actions CI
│   └── build-apk.yml          # 自动构建 APK 工作流
├── reference/                 # 参考文件（夸克扫描王样张、排盘参考图）
├── 前言.docx / 前言.pdf       # 算法与业务规范来源
├── 排盘.docx                  # 排盘结果标准样式参考
├── 完整的排盘.jpg             # 完整排盘参考图
├── 完善方向.txt               # 功能完善建议
├── AGENTS.md                  # 项目级 Agent 工作规范
├── capacitor.config.json      # Capacitor 配置
├── package.json               # npm 依赖与脚本
├── server.js                  # 本地预览服务器
└── 项目指南.md                # 本文件
```

### 4.2 核心文件交互链路

#### 链路 1：浏览器端排盘流程

```
public/index.html
    │
    ├─ 用户点击「开始排盘」
    │
    ▼
QiMenAlgorithm.fullPaiPan(y,m,d,h,min)
    │     （定义于 public/algorithm.bundle.js）
    ▼
algorithm/pillars.js
    ├─ getFourPillars() ──▶ lunar-javascript 计算四柱
    └─ fullPaiPanFromTime()
        ▼
    algorithm/qimen.js
        ├─ determinePan()     判定阳遁/阴遁、局数
        ├─ determineGuiShen() 定贵神
        ├─ arrangeDiGan()     排地盘三奇六仪
        ├─ arrangeShen()      排八神
        ├─ arrangeXing()      排九星
        ├─ arrangeMen()       排八门
        └─ arrangeTianGan()   排天盘干、暗干
        ▼
    返回完整排盘数据对象
        ▼
public/index.html
    ├─ renderResult()   渲染基础信息、四柱、遁局信息
    ├─ renderTraditionalPlate() 渲染 4×4 十三宫表格
    ├─ renderAnalysis() 渲染综合统计分析
    └─ saveHistory()    保存到 localStorage
```

#### 链路 2：本地开发预览流程

```
server.js
    │
    ├─ 启动 Express 监听 8080 端口
    ├─ 静态托管 public/ 目录
    └─ 提供 /api/paipan 接口
        │
        ▼
    algorithm/pillars.js ──▶ algorithm/qimen.js
        │
        ▼
    返回 JSON { ok, data }
        │
        ▼
    浏览器 public/index.html（开发模式下未加载 bundle 时调用）
```

#### 链路 3：历史记录与持久化

```
public/index.html
    │
    ├─ saveHistory(data) ──▶ localStorage.setItem('qimen_history')
    ├─ renderHistory()  ────▶ 读取 localStorage 渲染列表
    ├─ loadHistoryDirect(id) ▶ 从历史恢复并重新渲染
    └─ exportHistory()  ────▶ 导出 JSON 文件
```

#### 链路 4：PDF 导出流程

```
public/index.html exportPDF()
    │
    ├─ 读取当前 curData
    ├─ 生成可打印 HTML（含样式）
    ├─ Blob + URL.createObjectURL()
    └─ window.open(url, '_blank')
        │
        ▼
    系统浏览器打开打印页（APP 内调用外部浏览器）
```

#### 链路 5：APK 构建流程

```
GitHub Actions / 本地命令行
    │
    ├─ npm ci
    ├─ npm run build:browser        # esbuild 生成 algorithm.bundle.js
    ├─ npx cap sync android         # 同步 web 资源到 android/app/src/main/assets/public
    └─ cd android && ./gradlew assembleDebug
        │
        ▼
    输出 android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 五、关键数据对象

### 5.1 排盘结果对象（curData）

```js
{
  input: { year, month, day, hour, minute, name, gender, location, trueSolar, panType },
  pillars: { year, month, day, time, gan: {...}, zhi: {...} },
  pillarArr: ['丙午', '丙申', '庚申', '癸未'],
  pan: { pan: '阴盘', dun: '阳遁', ju: 5, ganSum, zhiSum },
  guiShen: { dayGan, isNight, zhi, luoshu },
  palaces: [
    { idx, pos, label, luoshu, shen, xing, men, tianGan, diGan, anGan },
    // ... 共 13 个宫位
  ],
  layout: [...],        // 十三宫空间布局
  luoshuCoords: {...}   // 洛书坐标映射
}
```

### 5.2 知识库对象（KNOWLEDGE）

```js
{
  shen: { '贵神': { meaning, analysis, class, keywords, advice }, ... },
  xing: { '贪狼': { meaning, analysis, class, keywords, advice }, ... },
  men: { '休': { meaning, analysis, class, keywords, advice }, ... },
  ganWuxing: { '甲': { wuxing, organ, emotion, direction }, ... },
  kongWang: { liuJia: {...} },
  shenSha: { yiMa: {...} }
}
```

---

## 六、构建与运行

### 6.1 本地开发预览

```bash
npm install
npm run build:browser   # 生成 public/algorithm.bundle.js
node server.js          # 访问 http://localhost:8080
```

### 6.2 本地构建 APK

前置条件：安装 JDK 17、Android SDK、配置 ANDROID_HOME。

```bash
npm install
npm run build:android:win   # Windows
# 或
npm run build:android       # Linux/Mac
```

APK 输出路径：
- Debug：`android/app/build/outputs/apk/debug/app-debug.apk`
- Release（未签名）：`android/app/build/outputs/apk/release/app-release-unsigned.apk`

### 6.3 通过 GitHub Actions 获取 APK

1. 推送代码到 GitHub `master` 分支。
2. 进入仓库 Actions → Build Android APK。
3. 下载构建产物中的 `app-debug.apk` 或 `app-release-unsigned.apk`。

---

## 七、注意事项

1. **算法例外**：当前十三宫排盘算法已按前言示例验证，但阳盘规则、真太阳时校准等后续仍需单独完善。
2. **浏览器 bundle**：`public/algorithm.bundle.js` 是构建产物，修改 `algorithm/` 后必须重新执行 `npm run build:browser`。
3. **历史数据**：历史记录使用浏览器 `localStorage` 存储，卸载 APP 或清除数据会丢失。
4. **签名发布**：正式上架需配置 Android 签名 keystore，可在 `android/app/build.gradle` 中配置 `release` 签名的 `storeFile`、`storePassword`、`keyAlias`、`keyPassword`。
