# 学堂离线电子书库 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将现有学堂占位书单升级为首次下载、永久离线保存、可续读的 EPUB 电子书库。

**Architecture:** 使用固定合法书目清单驱动下载，以 IndexedDB 保存 EPUB Blob、校验信息和阅读进度，使用本地 EPUB.js 在 Capacitor WebView 中渲染。学堂逻辑与现有单文件页面解耦为独立脚本和样式，并同步根目录、`public/`、`docs/` 三套入口。

**Tech Stack:** HTML/CSS/JavaScript、IndexedDB、Web Crypto SHA-256、EPUB.js、JSZip、Capacitor Android 8。

**Spec:** `学堂模块完整方案.md`

## Global Constraints

- 只收录公版、开放许可或明确授权资源。
- 首批书目不少于 8 本。
- 书籍不随 APK 打包，首次下载后保存在设备本地。
- 不覆盖当前工作区已有的排盘算法和十三宫界面修改。
- 前端页面标题统一使用“道家奇门遁甲”。
- 每次工作完成必须更新 `work-flow.md`。

---

### Task 1: 书目清单与纯函数

**Files:**
- Create: `school-library.js`
- Create: `tests/school-library.test.js`

**Interfaces:**
- Produces: `SCHOOL_CATALOG`、`validateCatalog(catalog)`、`isValidEpub(bytes, contentType)`、`calculateSha256(buffer)`。

- [ ] **Step 1: 写书目数量、唯一性和 EPUB 校验失败测试**
- [ ] **Step 2: 运行测试，确认因模块不存在而失败**
- [ ] **Step 3: 实现 8 本书清单及最小纯函数**
- [ ] **Step 4: 运行测试，确认通过**

### Task 2: IndexedDB 存储与下载状态机

**Files:**
- Modify: `school-library.js`
- Modify: `tests/school-library.test.js`

**Interfaces:**
- Produces: `SchoolBookStore`、`BookDownloadManager`、`getBook(id)`、`saveBook(record)`、`saveProgress(progress)`、`getProgress(bookId)`。

- [ ] **Step 1: 写保存、恢复、失败不落库、进度恢复测试**
- [ ] **Step 2: 运行测试，确认预期失败**
- [ ] **Step 3: 实现 IndexedDB 适配器和可注入 fetch/store 的下载管理器**
- [ ] **Step 4: 运行测试，确认通过**

### Task 3: EPUB 阅读器

**Files:**
- Create: `school-reader.js`
- Create: `school.css`
- Add: `vendor/epub.min.js`
- Add: `vendor/jszip.min.js`
- Create: `tests/school-reader.test.js`

**Interfaces:**
- Consumes: `SchoolBookStore.getBook()`、`getProgress()`、`saveProgress()`。
- Produces: `SchoolReader.open(bookId)`、`close()`、`setTheme(theme)`、`setFontScale(scale)`。

- [ ] **Step 1: 写阅读器状态、主题、字号和进度节流测试**
- [ ] **Step 2: 运行测试，确认预期失败**
- [ ] **Step 3: 安装并复制本地 EPUB.js/JSZip 浏览器构建**
- [ ] **Step 4: 实现阅读器 UI 和进度恢复**
- [ ] **Step 5: 运行测试，确认通过**

### Task 4: 学堂页面接入

**Files:**
- Modify: `index.html`
- Modify: `public/index.html`
- Modify: `docs/index.html`
- Copy: `school-library.js`、`school-reader.js`、`school.css`、`vendor/*` to `public/` and `docs/`

**Interfaces:**
- Consumes: `SCHOOL_CATALOG`、`BookDownloadManager`、`SchoolReader`。
- Produces: 可交互书架、下载进度、失败重试、离线提示和阅读入口。

- [ ] **Step 1: 写静态接入检查，确认当前页面仍含占位 `SCHOOL_BOOKS`**
- [ ] **Step 2: 运行检查，确认失败**
- [ ] **Step 3: 移除占位书单并接入独立模块**
- [ ] **Step 4: 同步三套前端入口并运行检查**

### Task 5: 真实资源下载与许可清单

**Files:**
- Create: `data/school-books.json`
- Create: `data/school-books-lock.json`
- Create: `docs/学堂书目与许可.md`

**Interfaces:**
- Consumes: 8 个固定维基文库作品页和 EPUB 导出地址。
- Produces: 经 HTTP/MIME/ZIP 头校验的书目、实测大小和 SHA-256 锁文件。

- [ ] **Step 1: 下载 8 本 EPUB 到临时验证目录**
- [ ] **Step 2: 校验文件类型、ZIP 头、大小并计算 SHA-256**
- [ ] **Step 3: 写入运行时清单、锁文件和许可说明**
- [ ] **Step 4: 删除临时下载文件，确保 EPUB 未被打进 APK**

### Task 6: 浏览器与 Android 验证

**Files:**
- Modify: `work-flow.md`
- Output: `app-debug.apk`

**Interfaces:**
- Consumes: 完成的学堂前端与现有 Capacitor 工程。
- Produces: 通过自动测试、浏览器视觉检查和 Android 构建的交付物。

- [ ] **Step 1: 运行学堂测试和现有算法测试**
- [ ] **Step 2: 启动本地页面并检查移动端/桌面端布局**
- [ ] **Step 3: 使用 Computer Use 验证书籍下载、打开、翻页、字号和断点续读**
- [ ] **Step 4: 同步 Capacitor 并构建 debug APK**
- [ ] **Step 5: 检查 APK 大小，确认未内置 EPUB**
- [ ] **Step 6: 更新 `work-flow.md` 并核对最终差异**
