# 十三宫奇门遁甲 APP - 后端用户端完善方向

> 本文档汇总 APP 用户端后端所需全部功能模块，并标注实现状态与关键接口。
> 对应实现代码位于 `backend/` 目录，`server.js` 统一挂载路由。

---

## 一、账号与认证

### 功能点
- 手机号 + 短信验证码注册/登录
- 账号密码注册/登录
- 密码找回/重置
- 微信/QQ 第三方授权登录（占位，需配置开放平台）
- Token 多设备管理

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/sms-code` | 发送短信验证码（当前为测试码 123456） |
| POST | `/api/auth/login-by-sms` | 验证码快捷登录 |
| POST | `/api/auth/register` | 手机号密码注册 |
| POST | `/api/auth/login` | 手机号密码登录 |
| POST | `/api/auth/reset-password` | 验证码重置密码 |
| POST | `/api/auth/login-by-third` | 微信/QQ 第三方登录 |
| POST | `/api/auth/logout` | 登出并吊销当前 Token |

### 状态
已实现基础版，短信与第三方登录为占位实现，生产环境需接入真实服务商。

---

## 二、用户资料

### 功能点
- 昵称、头像、性别
- 出生年月日时
- 常住地/出生地
- 会员等级与有效期
- 账户创建时间、最后登录时间

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/user/profile` | 获取个人资料 |
| PUT | `/api/user/profile` | 更新个人资料 |

### 状态
已实现。

---

## 三、排盘数据

### 功能点
- 历史排盘记录存储（时间、四柱、遁局、十三宫结果）
- 收藏/标记重要排盘
- 排盘记录搜索、筛选、排序
- 排盘结果持久化与快照
- 排盘记录云端保存
- 历史记录删除（软删除）

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/history` | 保存排盘记录 |
| GET | `/api/history` | 列表查询（支持 keyword/favorite/sort/page/size） |
| GET | `/api/history/:id` | 单条详情 |
| PUT | `/api/history/:id` | 更新标题/收藏/标签/备注 |
| DELETE | `/api/history/:id` | 软删除 |

### 状态
已实现。

---

## 四、支付与会员

### 功能点
- 充值与消费记录
- 会员套餐（月卡/年卡/永久）
- AI 对话次数额度
- 高级功能解锁占位（导出 PDF、批量排盘、AI 解析）

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/payment/plans` | 会员套餐列表 |
| POST | `/api/payment/orders` | 创建订单 |
| POST | `/api/payment/orders/:id/pay` | 模拟支付回调，开通会员并增加额度 |
| GET | `/api/payment/orders` | 我的订单 |

### 状态
已实现套餐、订单与模拟支付；生产环境需替换为微信支付/支付宝真实回调。

---

## 五、AI 智能体服务

### 功能点
- 对话记录云端保存
- 对话上下文与当前排盘关联（history_id / session_id）
- AI 调用次数统计与限流
- 用户反馈（点赞/点踩）

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/ai/quota` | 查询剩余额度与今日使用次数 |
| POST | `/api/ai/chat` | AI 对话，自动扣减额度并保存记录 |
| GET | `/api/ai/chats` | 对话历史列表 |
| POST | `/api/ai/feedback` | 对话反馈 |

### 状态
已实现。`/api/chat` 保留为无登录兼容入口。

---

## 六、通知与运营

### 功能点
- 系统公告
- 排盘提醒/每日运势推送（占位）
- 消息中心
- 版本更新提示（占位）

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/notification/announcements` | 系统公告列表 |
| GET | `/api/notification/messages` | 我的消息 |
| POST | `/api/notification/messages/:id/read` | 标记已读 |

### 状态
基础公告与消息已实现；推送与版本更新需接入第三方推送/SDK。

---

## 七、安全与合规

### 功能点
- 密码 bcrypt 加密
- JWT + 数据库存储 Token 双校验
- 操作日志记录

### 实现
- `users.password_hash` 使用 bcrypt 存储
- `tokens` 表记录登录凭证，支持吊销
- `operation_logs` 表记录关键操作

### 状态
已实现。

---

## 八、数据统计

### 功能点
- 用户活跃度
- 排盘次数统计
- AI 调用统计

### 实现接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/stats` | 平台核心数据（需管理员手机号尾号 0000） |
| GET | `/api/admin/active-users` | 活跃用户排行 |

### 状态
已实现基础统计。

---

## 九、待完善项

1. **短信服务商接入**：将 `/api/auth/sms-code` 中的测试码替换为真实短信网关。
2. **第三方支付回调**：将 `/api/payment/orders/:id/pay` 替换为微信/支付宝异步通知。
3. **推送服务**：集成极光推送或类似服务，实现每日运势与排盘提醒。
4. **前端对接**：在 `public/index.html` 中接入登录态、历史记录同步、AI 额度展示。
5. **部署与运维**：选择云服务器/容器化部署，配置 HTTPS、数据库备份、日志监控。
