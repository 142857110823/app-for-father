# 其他功能-总规划执行提示词

> 用法：复制本文件“开始执行”以下全部内容，粘贴到本项目的新对话中。
> 本提示词将“其他功能——总规划.md”的仅规划状态切换为正式实施状态；仅对新对话生效，不改变历史记录。

## 开始执行

你是本项目的主开发 Agent。请在 Windows 工作区 `F:\1\夫` 内，直接执行“其他功能——总规划”的正式开发，不停留在建议、问卷或半成品方案阶段。

### 一、项目与仓库

- 项目名称：道家奇门遁甲 APP
- 工作区：`F:\1\夫`
- GitHub 仓库：`https://github.com/142857110823/app-for-father`
- 当前主分支：`master`
- GitHub Pages 预览候选：`https://142857110823.github.io/app-for-father/`
- 本地预览首选：`http://localhost:8090/`
- Pages 部署源以仓库当前实际配置为准；不得凭印象假设是根目录、`docs/` 或其他分支。

### 二、先读文件，再动手

开始任何代码修改前，必须按以下顺序读取并记录事实：

1. `AGENTS.md`
2. `work-flow.md`
3. `项目指南.md`
4. `其他功能——总规划.md`
5. `其他功能规划/四柱八字/规划.md`
6. `其他功能规划/紫微斗数/规划.md`
7. `其他功能规划/梅花易数/规划.md`
8. `其他功能规划/大六壬/规划.md`
9. `排盘-【阴盘-阳遁-5局】.docx`
10. `排盘-【阴盘-阴遁-5局】 2(1)(1).docx`
11. `天罡.docx`
12. `前言.docx`
13. 当前 `algorithm/`、`school-*.js`、`index.html`、`public/`、`docs/`、`tests/`、`package.json`

文档表格优先于文字描述；权威排盘文档优先于截图、旧脚本和代码推论。用户提供的错误截图只能作为问题反例，不能作为标准盘。

### 三、本次正式范围

“我的 → 其他”最终只保留四个功能：

1. 四柱八字：将“万年历”替换为四柱八字。
2. 紫微斗数：将“智能罗盘”替换为紫微斗数。
3. 梅花易数：新增时间起卦和数字起卦。
4. 大六壬：新增天地盘、四课、三传和天将。

同时完成以下清理：

- 删除“古籍查询”的入口、路由、事件、旧文案和无用资源。
- 古籍阅读只保留在“学堂”模块，不在“我的 → 其他”重复出现。
- 不破坏现有十三宫奇门排盘、学堂 EPUB 下载、阅读器和 AI 悬浮入口。

### 四、正式开发授权

本次用户已经明确授权正式执行，因此本提示词覆盖 `其他功能——总规划.md` 中“仅规划/禁止实施”的本轮边界。覆盖范围仅限本次四个功能的开发和必要入口替换，不得扩大到无关模块。

### 五、推荐技术决策

- 四个功能必须分别使用独立目录和独立规则包：

```text
features/
  calendar-core/
  bazi/
  ziwei/
  meihua/
  daliuren/
```

- 公共历法只通过 `calendar-core` 接口复用。
- 算法必须是纯函数；UI、持久化和 AI 解读不得写入算法核心。
- 默认离线优先；AI 只能解释已生成结果，不能参与排盘计算。
- 每条结果必须保存：原始输入、标准化输入、选项、算法版本、中间过程、最终结果和创建时间。
- 默认子初 23:00 换日；真太阳时可选且默认关闭；所有争议规则必须在设置和结果中显示。
- 第一版不做付费、专家认证、社交、云端批命和确定性预测承诺。

### 六、必须按此顺序实施

#### 阶段 0：基线和隔离

- 检查 Git 状态，不回退用户已有修改。
- 建立或确认本次开发分支；如项目规范要求留在 `master`，遵守项目规范。
- 先运行现有测试，记录基线失败，不得把既有失败归因到本次改动。
- 建立功能目录、规则版本文件和测试入口。

#### 阶段 1：公共历法核心

- 审计当前 `lunar-javascript` 版本和现有四柱计算。
- 封装公历、农历、节气、干支、时区、真太阳时和子时边界接口。
- 先写失败测试，再实现。
- 测试立春、节令、23:00、00:00、闰月、闰日、跨时区和真太阳时边界。

#### 阶段 2：四柱八字

- 在 `features/bazi/` 内实现四柱、藏干、十神、五行统计、月令、通根、透干、大运和流年。
- 不把未经冻结的综合旺衰分数伪装成传统定论。
- 输出推演过程和规则版本。
- 至少建立 30 个固定标准案例，包含节气和子时边界。

#### 阶段 3：梅花易数

- 在 `features/meihua/` 内实现时间起卦和数字起卦。
- 输出本卦、互卦、变卦、动爻、体卦、用卦、算式和五行关系。
- 余数为 0、六动爻、跨子时、跨农历月和非法输入必须有测试。
- 不输出确定性吉凶承诺。

#### 阶段 4：紫微斗数

- 在 `features/ziwei/` 内固定第一版三合基础盘口径。
- 实现命宫、身宫、五行局、十四主星、必要辅星、生年四化、大限和流年。
- 可以交叉核验成熟开源实现，但不得未经审计直接复制为唯一真值。
- 闰月、晚子时、四化表、大限顺逆和庙旺表必须写入规则版本。
- 至少建立 30 个固定标准盘。

#### 阶段 5：大六壬

- 在 `features/daliuren/` 内实现月将、天地盘、四课、三传和十二天将。
- 三传必须保存候选和规则命中轨迹。
- 覆盖贼克、比用、涉害、遥克、昴星、别责、八专、伏吟、反吟等分支。
- 至少建立 50 个固定标准课；大六壬必须最后开发和单独验收。

#### 阶段 6：入口和 UI

- 仅在四个算法均通过独立验收后，替换“我的 → 其他”入口。
- 最终入口数量必须严格等于 4。
- 删除“古籍查询”全部可见和不可见入口。
- 复用项目现有宣纸白、墨色、暗金、楷体和夸克扫描王式信息层级。
- 保持 360×800、412×915 和桌面视口无重叠、无溢出。

#### 阶段 7：回归、构建和部署

- 运行四功能测试、现有奇门测试、学堂测试和页面接入测试。
- 构建浏览器 bundle，保持根目录、`public/`、`docs/` 同步。
- 执行 `npx cap sync android` 并构建 APK。
- 检查 APK 不意外内置不应打包的电子书正文。
- 提交并推送 GitHub `master`，但不得声称 Pages 已刷新，除非 HTTP 实测确认。

### 七、TDD 和质量红线

- 每个新行为必须先写失败测试，确认测试确实失败，再写最小实现。
- 算法测试必须同时断言最终结果和关键中间过程。
- 不允许用截图、常量偏移、旋转猜测或“看起来相似”替代规则推导。
- 不允许把第三方库输出直接当作权威标准。
- 遇到失败先做系统性调试，不能连续盲改多个模块。
- 每个功能完成前，必须用独立规则核对、标准案例核对、UI 核对和离线核对。

### 八、每次工作结束的强制输出

每次工作结束前必须：

1. 更新 `work-flow.md`，记录时间、事件、问题来源、执行方向、执行边界、执行结果和相关文档。
2. 给出本次实际可访问的最新预览网址：
   - 优先给已验证可用的 GitHub Pages 地址；
   - 若 Pages 不可用，必须给已启动并验证过的本地地址，例如 `http://localhost:8090/`；
   - 同时给出备用地址和 HTTP 状态/验证时间；
   - 如果本次启动了新端口，必须保持服务运行到用户可以测试，除非明确说明已关闭。
3. 输出当前 Git 提交号、测试结果、APK 路径（如有）和未解决风险。
4. 不得只输出“完成”“已修复”而不给网址。

### 九、最终交付格式

最终回复必须按以下顺序：

1. 已完成的四个功能和入口变化。
2. 每个功能的算法版本、规则来源和测试数量。
3. 预览网址：
   - 主网址：
   - 备用网址：
   - 验证时间：
4. APK 路径和 SHA-256。
5. Git 提交号和推送状态。
6. 仍未确认的规则争议、第三方依赖风险或 Pages 部署风险。

现在开始执行。不要等待我批准，不要再次把本任务退回“只规划”。
